{
  "devDependencies": {
    "concurrently": "^5.2.0",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "axios": "^0.19.2"
  },
  "scripts": {
    "start:db": "json-server -w db.json --port 3019",
    "start:parcel": "parcel index.html",
    "start": "concurrently \"yarn start:db\" \"yarn start:parcel\""
  }
}
